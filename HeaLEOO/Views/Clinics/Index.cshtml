@model IEnumerable<ClinicVM>
@{
    ViewData["Title"] = "Clinics";
}

<div class="clinics-container">
    <div class="clinics-header">
        <h2 class="clinics-page-title">Clinics</h2>
        @if (User.IsInRole("Medical Assistant"))
        {
            <a asp-action="Create" class="clinics-btn-create">+ Add New Clinic</a>
        }
    </div>

    <div class="clinics-grid">
        @foreach (var clinic in Model)
        {
            <div class="clinic-card">
                <div class="clinic-photo">
                    @if (!string.IsNullOrEmpty(clinic.PhotoUrl))
                    {
                        <img src="@clinic.PhotoUrl" alt="@clinic.Name" />
                    }
                    else
                    {
                        <div class="clinic-photo-placeholder">No Image</div>
                    }
                </div>

                <div class="clinic-info">
                    <h3 class="clinic-name">@clinic.Name</h3>
                    <p class="clinic-address">@clinic.Address</p>
                    <p class="clinic-phone">@clinic.PhoneNumber</p>
                </div>

                <div class="clinic-actions">
                    <a asp-action="Details" asp-route-id="@clinic.Id" class="clinics-btn-details">Details</a>

                    @if (User.IsInRole("Medical Assistant"))
                    {
                        <a href="@Url.Action("Delete", new { id = clinic.Id })"
                           class="clinics-btn-delete"
                           onclick="return confirmClinicDelete(event, this.href, this.closest('.clinic-card'));">
                            Delete
                        </a>
                    }
                </div>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script src="~/js/clinic-Index.js"></script>
}
