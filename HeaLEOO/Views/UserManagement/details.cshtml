@* @using HeaLEOO.DTOs
@model AppUserDto

@{
    ViewData["Title"] = "User Details";
}

<div class="container py-5">
    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-primary text-white text-center rounded-top-4">
            <h3>👤 User Details</h3>
        </div>

        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-4 text-end fw-bold">User Name:</dt>
                <dd class="col-sm-8">@Model.UserName</dd>

                <dt class="col-sm-4 text-end fw-bold">Email:</dt>
                <dd class="col-sm-8">@Model.Email</dd>
            </dl>
        </div>

        <div class="card-footer text-center">
            <a asp-action="Index" class="btn btn-secondary rounded-pill px-4">⬅ Back</a>
            <a asp-action="Delete" asp-route-id="@Model.Id"
               class="btn btn-danger rounded-pill px-4">🗑 Delete</a>
        </div>
    </div>
</div> *@


@using HeaLEOO.DTOs
@model AppUserDto

@{
    ViewData["Title"] = "User Details";
}

<div class="user-details-container">
    <div class="user-details-card">
        <h2 class="user-details-name">@Model.UserName</h2>

        <div class="user-details-section">
            <h3>Email</h3>
            <p>@Model.Email</p>
        </div>

        <div class="user-details-actions">
            <a asp-action="Index" class="user-btn-back">Back</a>
        </div>
    </div>
</div>
