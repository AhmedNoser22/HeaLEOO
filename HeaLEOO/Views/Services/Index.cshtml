@model IEnumerable<HeaLEOO.ViewModels.ServiceVM>

@{
    ViewData["Title"] = "Services";
}

<div class="services-container">
    <div class="services-header">
        <h2 class="services-title">Services</h2>
        <a asp-action="Create" class="service-btn-create">+ Add Service</a>
    </div>

    <div class="services-grid">
        @foreach (var service in Model)
        {
            <div class="service-card">
                <div class="service-info">
                    <h3 class="service-name">@service.Name</h3>

                    <h6 class="service-label">Price:</h6>
                    <p class="service-address">@service.Price</p>

                    <h6 class="service-label">Clinic:</h6>
                    <p class="service-phone">@service.ClinicName</p>
                </div>
                   <div class="service-actions">
                        <a asp-action="Details" asp-route-id="@service.Id" class="service-btn-details">Details</a>
                    <a asp-action="Edit" asp-route-id="@service.Id" class="service-btn-edit">Edit</a>

                    <form asp-action="Delete" asp-route-id="@service.Id" method="post" style="display:inline"
                          onsubmit="return confirmServiceDelete(event, this);">
                        <button type="submit" class="service-btn-delete">Delete</button>
                    </form>
                   </div>     
            </div>
        }
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="~/js/service-Index.js"></script>
}
